# Built application files and build output
build/
/captures
*.apk
*.aar
*.ap_
*.aab

# Gradle
.gradle/
# NOTE: do NOT ignore the Gradle wrapper — gradlew, gradlew.bat, and
# gradle/wrapper/* must be committed so others can build the project.

# Kotlin
.kotlin/

# Local configuration (machine-specific SDK path — never commit)
local.properties

# Signing keys and secrets — NEVER commit these
*.jks
*.keystore
keystore.properties

# Android Studio / IntelliJ
*.iml
.idea/

# Native build
.externalNativeBuild
.cxx

# OS / misc
.DS_Store
*.log
